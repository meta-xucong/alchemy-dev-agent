const state={projects:[],project:null,remote:null,remoteTask:null,localRun:null,poll:null};
const $=id=>document.getElementById(id);
async function api(path,{method="GET",body}={}){const response=await fetch(path,{method,headers:body?{"Content-Type":"application/json"}:{},body:body?JSON.stringify(body):undefined});const data=await response.json();if(!response.ok)throw new Error(data.error?.message||data.error||"请求未完成");return data}
function escapeHtml(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}
function projectTitle(project){return String(project?.objective||"未命名项目").split(/[。.!！?？\n]/)[0].slice(0,34)||"未命名项目"}
function statusText(value){return({intake_ready:"准备开始",queued:"等待开始",running:"正在开发",paused:"已暂停",done:"已完成",completed:"已完成",failed:"需要处理",blocked:"需要处理"})[String(value||"").toLowerCase()]||"准备中"}
function setDialog(id,open){const dialog=$(id);if(open&&!dialog.open)dialog.showModal();if(!open&&dialog.open)dialog.close()}
async function loadProjects(){const data=await api("/projects");state.projects=data.projects||[];renderProjects();if(!state.project&&state.projects[0])await openProject(state.projects[0].project_id)}
function renderProjects(){const root=$("projectList");root.innerHTML=state.projects.length?state.projects.map(project=>`<button class="project-card ${state.project?.project_id===project.project_id?"active":""}" data-project="${escapeHtml(project.project_id)}"><strong>${escapeHtml(projectTitle(project))}</strong><span>${escapeHtml(statusText(project.latest_run_status||project.status))}</span></button>`).join(""):'<p class="connection-chip">还没有项目</p>';root.querySelectorAll("[data-project]").forEach(button=>button.addEventListener("click",async()=>{await openProject(button.dataset.project);setDialog("projectQuickDialog",true)}))}
async function openProject(projectId){clearPolling();const data=await api(`/projects/${encodeURIComponent(projectId)}`);state.project=data.project||data;const title=projectTitle(state.project);$("workspaceTitle").textContent=title;$("quickProjectTitle").textContent=title;$("quickProjectHint").textContent="打开项目继续开发，或直接把修改想法发给 Remote Codex。";$("emptyState").hidden=true;$("projectHome").hidden=false;$("projectObjective").textContent=state.project.objective||"未命名项目";$("projectStatus").textContent=statusText(state.project.status);$("projectHint").textContent="选择开发方式；之后所有进展都会集中在这个项目里。";renderProjects();await loadLatestRun();}
async function loadLatestRun(){const summary=state.projects.find(item=>item.project_id===state.project?.project_id);const runId=summary?.latest_run_id;if(!runId){renderIdle();return}try{state.localRun=await api(`/projects/${state.project.project_id}/runs/${runId}/status`);renderLocalProgress(state.localRun);if(["queued","running","paused"].includes(String(state.localRun.status).toLowerCase()))startPolling()}catch{renderIdle()}}
function renderIdle(){$("progressStatus").textContent="尚未开始";$("progressDot").className="progress-dot";$("progressTitle").textContent="等待开始";$("progressMessage").textContent="选择 Alchemy 或 Remote Codex，开始你的开发任务。";$("progressSteps").innerHTML="";$("deliveryCard").textContent="还没有交付结果。"}
function renderLocalProgress(run){const phase=run.current_phase||run.status||"running";const status=String(run.status||"").toLowerCase();$("progressStatus").textContent=statusText(status);$("progressDot").className=`progress-dot ${["done","completed"].includes(status)?"done":status==="failed"||status==="blocked"?"error":"running"}`;$("progressTitle").textContent=status==="done"?"开发完成":status==="failed"?"开发遇到问题":"Alchemy 正在开发";$("progressMessage").textContent=run.summary||`当前正在：${phase}`;const nodes=run.roadmap_progress?.phases||[];$("progressSteps").innerHTML=nodes.slice(-5).map(item=>`<li class="${item.status==="running"?"active":""}">${escapeHtml(item.title||item.phase_id||"处理中")}</li>`).join("");const delivery=run.delivery||run.delivery_report;if(delivery?.ready_for_review||status==="done")$("deliveryCard").innerHTML="开发已完成。可以打开项目结果并开始验收。"}
async function startAlchemy(){if(!state.project)return;$("progressTitle").textContent="正在启动 Alchemy";$("progressMessage").textContent="正在准备开发环境…";try{const result=await api(`/projects/${state.project.project_id}/runs`,{method:"POST",body:{async:true,real_codex:true,isolate_real_run:true,keep_worktree:true}});state.localRun={status:"queued",run_id:result.run_id,summary:"开发任务已创建。"};renderLocalProgress(state.localRun);startPolling()}catch(error){showProgressError(error.message)}}
async function loadRemote(){state.remote=await api("/integrations/remote-codex");const chip=$("remoteStatus");chip.textContent=state.remote.connected?"Remote Codex 已连接":"Remote Codex 未连接";chip.classList.toggle("connected",Boolean(state.remote.connected));}
function openRemote(){if(!state.project)return;if(!state.remote?.connected){$("remoteUrl").value=state.remote?.base_url||"http://127.0.0.1:8765";setDialog("connectionDialog",true);return}$("conversationTitle").textContent=projectTitle(state.project);setDialog("remoteDialog",true);renderConversation();}
function renderConversation(){const feed=$("conversationFeed");const detail=state.remoteTask;if(!detail){feed.innerHTML='<div class="message assistant"><small>Remote Codex</small>你好，我已准备好帮你继续开发这个项目。告诉我下一步要做什么。</div>';return}const messages=detail.messages||[];feed.innerHTML=messages.map(item=>`<div class="message ${item.role==="user"?"user":"assistant"}"><small>${item.role==="user"?"你":"Remote Codex"}</small>${escapeHtml(item.text||"")}</div>`).join("")||'<div class="message assistant">正在连接对话…</div>';feed.scrollTop=feed.scrollHeight;renderRemoteProgress(detail)}
function renderRemoteProgress(detail){const task=detail.task||{};const terminal=Boolean(detail.is_terminal);$("progressStatus").textContent=detail.status_label||statusText(task.state);$("progressDot").className=`progress-dot ${terminal?task.state==="completed"?"done":"error":"running"}`;$("progressTitle").textContent=terminal?detail.status_label||"Remote Codex 已完成":"Remote Codex 正在开发";$("progressMessage").textContent=detail.answer||detail.progress?.at(-1)?.label||"正在同步进度…";$("progressSteps").innerHTML=(detail.progress||[]).slice(-5).map(item=>`<li class="${item.state==="active"?"active":""}">${escapeHtml(item.label)}</li>`).join("")}
async function sendConversation(event){event.preventDefault();const message=$("conversationMessage").value.trim();if(!message||!state.project)return;$("conversationMessage").value="";try{const result=await api(`/projects/${state.project.project_id}/remote-codex/tasks`,{method:"POST",body:{message,parent_task_id:state.remoteTask?.task?.task_id||""}});state.remoteTask={task:result.task,messages:[{role:"user",text:message}],progress:[],is_terminal:false};renderConversation();startPolling()}catch(error){$("conversationHelp").textContent=error.message}}
async function refreshRemoteTask(){const taskId=state.remoteTask?.task?.task_id;if(!taskId||!state.project)return;try{state.remoteTask=await api(`/projects/${state.project.project_id}/remote-codex/tasks/${encodeURIComponent(taskId)}`);renderConversation();if(state.remoteTask.is_terminal)clearPolling()}catch(error){$("conversationHelp").textContent=error.message;clearPolling()}}
function startPolling(){clearPolling();state.poll=window.setInterval(async()=>{if(state.remoteTask)await refreshRemoteTask();else if(state.localRun?.run_id&&state.project){state.localRun=await api(`/projects/${state.project.project_id}/runs/${state.localRun.run_id}/status`);renderLocalProgress(state.localRun);if(!["queued","running","paused"].includes(String(state.localRun.status).toLowerCase()))clearPolling()}},1800)}function clearPolling(){if(state.poll)window.clearInterval(state.poll);state.poll=null}
function showProgressError(message){$("progressStatus").textContent="需要处理";$("progressDot").className="progress-dot error";$("progressTitle").textContent="暂时无法开始";$("progressMessage").textContent=message}
async function createProject(event){event.preventDefault();const objective=$("projectObjectiveInput").value.trim();const name=$("projectName").value.trim();if(!objective)return;const created=await api("/projects",{method:"POST",body:{objective:`${name}：${objective}`,primary_input_mode:"one_line",expand_one_line:true}});setDialog("projectDialog",false);await loadProjects();await openProject(created.project.project_id);event.target.reset()}
async function saveConnection(event){event.preventDefault();$("connectionMessage").textContent="正在检查连接…";try{state.remote=await api("/integrations/remote-codex",{method:"POST",body:{base_url:$("remoteUrl").value}});await loadRemote();$("connectionMessage").textContent=state.remote.message||"已保存";if(state.remote.connected){setTimeout(()=>{setDialog("connectionDialog",false);openRemote()},350)}}catch(error){$("connectionMessage").textContent=error.message}}
function bind(){$("newProject").addEventListener("click",()=>setDialog("projectDialog",true));$("emptyNewProject").addEventListener("click",()=>setDialog("projectDialog",true));$("projectForm").addEventListener("submit",createProject);$("refreshProjects").addEventListener("click",loadProjects);$("connectRemote").addEventListener("click",()=>{ $("remoteUrl").value=state.remote?.base_url||"http://127.0.0.1:8765";setDialog("connectionDialog",true)});$("connectionForm").addEventListener("submit",saveConnection);$("openConversation").addEventListener("click",openRemote);$("startAlchemy").addEventListener("click",startAlchemy);$("closeRemote").addEventListener("click",()=>setDialog("remoteDialog",false));$("closeProjectQuick").addEventListener("click",()=>setDialog("projectQuickDialog",false));$("quickOpenWorkspace").addEventListener("click",()=>setDialog("projectQuickDialog",false));$("quickOpenConversation").addEventListener("click",()=>{setDialog("projectQuickDialog",false);openRemote()});$("conversationForm").addEventListener("submit",sendConversation)}
async function init(){bind();await Promise.all([loadRemote(),loadProjects()])}init().catch(error=>{ $("emptyState").querySelector("p").textContent=`无法载入工作台：${error.message}`})
